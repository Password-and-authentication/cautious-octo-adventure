const { makeShip, board1ships, getShip } = require("./ships");
import { boards } from "./Board";
import { aiRandom, Player, random } from "./Player";
import {game, board1, player1, board2, player2} from "./game"
import {container, numbers, boardOne, boardTwo} from "./UI"
import "./style.css"

/*
const ship = makeShip(1, 1)
const ship2 = makeShip(2, 2)
const ship3 = makeShip(1, 3)
const ship4 = makeShip(4, 4)
*/











































